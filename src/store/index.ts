// store - dispatch - selector
export { store, useAppDispatch, useAppSelector, persistor } from "./store";

// state types
export type {
  UserDetailsState,
  RootState,
  UserRolesState,
} from "./tools/types";
